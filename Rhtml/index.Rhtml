<!DOCTYPE html>
<html>
<head>
<title>Algorithm selection scenarios</title>
</head>
<body>


<h1>Algorithm Selection Benchmark Library</h1>

<br>

<table border=1 cellpadding=10>
<tr>
  <th>Scenario Name</th>
  <th>Instances</th>
  <th>Algorithms</th>
  <th>Features</th>
  <th>Type</th>
  <th>Stoch. Feat.</th>
  <th>Stoch. Alg.</th>
  <th>Feature Costs</th>
</tr>

<!--begin.rcode, echo = FALSE, results = "asis"

printTDString = function(x) {
  catf("<td align='right'>"); catf(x); catf("</td>")
}
printTDNumber = function(x) {
  printTDString(as.character(x))
}
printTDLogical = function(x) {
  printTDString(as.character(x))
}


for (i in seq_along(asscenarios)) {
  asscenario = asscenarios[[i]]
  desc = asscenario$desc
  scenario.dir = scenario.dirs[i]
  bn = basename(scenario.dir)
  url = sprintf("%s/%s/index.html", url.prefix, bn)
  catf("<tr>")
  catf("<td>"); catf("<a href='%s'>%s</a>", url, desc$scenario_id); catf("</td>")
  printTDNumber(length(getInstanceNames(asscenario)))
  printTDNumber(length(getAlgorithmNames(asscenario)))
  printTDNumber(length(getFeatureNames(asscenario)))
  printTDString(asscenario$desc$performance_type[[1]])
  printTDString(sprintf("%s, max. reps = %i",
    length(asscenario$desc$features_stochastic) > 0L, max(asscenario$feature.values$repetition)))
  printTDString(sprintf("%s, max. reps = %i",
    length(asscenario$desc$algorithms_stochastic) > 0L, max(asscenario$algo.runs$repetition)))
  printTDLogical(!is.null(asscenario$feature.costs))
  catf("</tr>")
}

end.rcode-->
</table>

</body>
</html>

