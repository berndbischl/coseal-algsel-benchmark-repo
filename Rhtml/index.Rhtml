<!DOCTYPE html>
<html>
<head>
<title>Algorithm selection tasks</title>
</head>
<body>


<h1>Algorithm Selection Benchmark Library</h1>

<b>
CAUTION: Heavily work in progress, do not use data or results for papers until we remove
this message. ETA: April 2014.
</b>

<br><br><br>

<table border=1 cellpadding=10>
<tr>
  <th>Task Name</th>
  <th>Instances</th>
  <th>Algorithms</th>
  <th>Features</th>
  <th>Type</th>
  <th>Stoch. Feat.</th>
  <th>Stoch. Alg.</th>
  <th>Feature Costs</th>
</tr>

<!--begin.rcode, echo = FALSE, results = "asis"

printTDString = function(x) {
  catf("<td align='right'>"); catf(x); catf("</td>")
}
printTDNumber = function(x) {
  printTDString(as.character(x))
}
printTDLogical = function(x) {
  printTDString(as.character(x))
}


for (i in seq_along(astasks)) {
  astask = astasks[[i]]
  desc = astask$desc
  task.dir = task.dirs[i]
  bn = basename(task.dir)
  url = sprintf("%s/%s/index.html", url.prefix, bn)
  catf("<tr>")
  catf("<td>"); catf("<a href='%s'>%s</a>", url, desc$task_id); catf("</td>")
  printTDNumber(length(getInstanceNames(astask)))
  printTDNumber(length(getAlgorithmNames(astask)))
  printTDNumber(length(getFeatureNames(astask)))
  printTDString(astask$desc$performance_type[[1]])
  printTDString(sprintf("%s, max. reps = %i",
    length(astask$desc$features_stochastic) > 0L, max(astask$feature.values$rep)))
  printTDString(sprintf("%s, max. reps = %i",
    length(astask$desc$algorithms_stochastic) > 0L, max(astask$algo.runs$rep)))
  printTDLogical(!is.null(astask$feature.costs))
  catf("</tr>")
}

end.rcode-->
</table>

</body>
</html>

